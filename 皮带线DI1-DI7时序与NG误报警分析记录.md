# 皮带线 DI1→DI7 时序与 NG 误报警分析记录

> 整理日期：2026-09-02  
> 用途：归档现场原始日志、时序统计、故障判断依据和后续标定方法。  
> 适用范围：当前皮带速度、当前 DI1/DI7 安装位置及本次测试订单。参数正式投产前仍需在最差工况复测。

## 1. 结论摘要

1. DI1 到 DI7 的距离由现场确认为约 `260 mm`。
2. 较快订单的视觉检测耗时为 `169～290 ms`，DI1→DI7 运行时间为 `953～1094 ms`。
3. 较慢订单的视觉检测耗时为 `619～886 ms`，虽然有 17 次在 DI1 停车等待结果，但恢复运行后的 DI1→DI7 时间仍为 `860～1016 ms`。
4. 两类订单合并后的 59 个正常 DI7 确认样本范围为 `860～1094 ms`，说明视觉检测时间不会直接进入出口运行计时，不需要按订单分别设置 DI1→DI7 窗口。
5. 现场 `item24 NG → item25 GOOD` 的报警属于出口信号误归属：DI7 在相对 NG 的 `1938 ms`、相对 GOOD 的 `1000 ms` 时触发。`1000 ms`符合正常运输时间，而 `1938 ms`是因为 NG 防错上限 `2000 ms`过宽，导致两个候选窗口重叠。
6. 基于当前数据，建议第一轮试验使用统一窗口 `500～1250 ms`。其中最关键的是把 `waste_outlet_arrival_max_run_ms` 从 `2000 ms`缩短到 `1250 ms`。
7. 日志中还出现两次 `FIFO_UNDERFLOW` 和多次无候选 DI7，说明除出口窗口外，还需要排查 DI0 漏计数、DI1 多触发、产品间距过小、残留产品或人工中途放料。

## 2. 原始数据文件

完整原始日志保留在项目根目录，本文只提取与时序和故障有关的记录，不复制相机预览渲染等无关明细。

| 文件 | 大小 | 最后修改时间 | SHA-256 |
|---|---:|---|---|
| [runtime_camera - 副本.log](<runtime_camera - 副本.log>) | 45,727 字节 | 2026-09-02 11:32:47 | `FF1FD82DD540A5FD323EEF426CAE3BED657424C9C6EE539A813A4BDB83755C69` |
| [runtime_camera - 副本 (2).log](<runtime_camera - 副本 (2).log>) | 161,824 字节 | 2026-09-02 13:12:56 | `A9768B89859C3407DD895393F3399173EAA4E5D9CD52C91CA9F877239C6CC244` |

注意：第二份日志包含第一份日志的旧内容。因此统计时采用以下不重叠区间：

- 较快订单：2026-09-02 11:31:32～11:32:59。
- 较慢订单：2026-09-02 13:00:00～13:13:00。

## 3. 信号与日志字段定义

### 3.1 信号含义

| 信号 | 含义 |
|---|---|
| DI0 | 相机位置检测到产品，创建产品编号并触发检测 |
| DI1 | 产品到达剔除位置；GOOD放行，NG启动吹气 |
| DI7 | GOOD出料口传感器；同时用于发现未被吹走、误入GOOD通道的NG |
| DI8 | NG废料出口传感器；启用逐件确认时用于确认NG已经进入废料通道 |

### 3.2 关键日志字段

| 字段 | 含义 |
|---|---|
| `duration_ms` | 视觉采图、匹配和推理的总耗时，不是皮带运输时间 |
| `inspection_wait_ms` | 产品已到DI1，但检测结果尚未完成时的停车等待时间 |
| `di1_motion_ms` | 产品建立出口跟踪时的累计皮带运动时间 |
| `travel_ms` | 从该产品建立DI1出口跟踪到DI7确认之间的皮带运动时间 |
| `elapsed_ms` | 当前事件相对某个候选产品DI1时刻的运动时间 |
| `window_ms` | 允许该产品到达出口或进行NG防错的时间窗口 |
| `active_candidates` | 当前出口信号可能关联的在途记录及其相对时间 |

当前日志中的 `active_candidates` 会显示已经建立且尚未超过上限的记录。对于同出口的正常确认，还需检查 `elapsed_ms` 是否达到窗口下限。例如 `elapsed_ms=15 ms` 的后续GOOD虽然出现在列表中，但尚未达到 `500 ms`下限，不能被确认。

## 4. 较快订单数据

### 4.1 统计结果

| 指标 | 样本数 | 最小值 | 最大值 | 平均值 | 中位数 |
|---|---:|---:|---:|---:|---:|
| 视觉检测耗时 | 25 | 169 ms | 290 ms | 273.0 ms | 276 ms |
| DI1停车等待 | 0 | — | — | — | — |
| 正常DI1→DI7 | 21 | 953 ms | 1094 ms | 1037.2 ms | 1047 ms |

### 4.2 原始 DI1→DI7 样本

```text
item22=1094, item23=953, item27=1000, item28=1078, item29=1078,
item30=1078, item31=1063, item32=1032, item33=969, item34=1094,
item35=1063, item36=1000, item37=1047, item38=968, item39=984,
item40=1031, item41=1062, item42=1047, item43=1093, item44=985,
item46=1063
```

单位均为毫秒。

### 4.3 原始视觉检测耗时样本

```text
290, 274, 169, 284, 276, 276, 277, 277, 279, 276,
277, 275, 276, 284, 274, 274, 280, 272, 275, 280,
278, 276, 274, 275, 278
```

## 5. 较慢订单数据

### 5.1 统计结果

| 指标 | 样本数 | 最小值 | 最大值 | 平均值 | 中位数 |
|---|---:|---:|---:|---:|---:|
| 视觉检测耗时 | 72 | 619 ms | 886 ms | 670.1 ms | 657 ms |
| DI1停车等待 | 17 | 46 ms | 157 ms | 90.9 ms | 78 ms |
| 正常DI1→DI7 | 38 | 860 ms | 1016 ms | 904.2 ms | 891 ms |

### 5.2 原始 DI1 停车等待样本

```text
125, 94, 62, 140, 125, 63, 47, 78, 157,
141, 62, 125, 46, 62, 94, 63, 62
```

单位均为毫秒。

### 5.3 原始 DI1→DI7 样本

第二份日志包含多次清线和重新开始，因此产品编号会从1重新计数。以下按日志出现顺序保存：

```text
984, 984, 969, 984, 984, 922, 875, 891, 875, 891,
890, 875, 906, 890, 860, 875, 875, 891, 860, 875,
891, 875, 891, 890, 985, 891, 875, 875, 890, 907,
891, 875, 875, 890, 922, 891, 875, 1016
```

单位均为毫秒。

### 5.4 原始视觉检测耗时样本

```text
886, 742, 714, 744, 762, 787, 746, 738, 658, 661,
656, 757, 676, 728, 685, 678, 626, 621, 669, 619,
704, 647, 632, 655, 631, 640, 644, 662, 625, 640,
657, 662, 633, 663, 648, 650, 653, 701, 659, 642,
640, 654, 663, 640, 649, 682, 636, 663, 651, 655,
684, 651, 643, 630, 637, 659, 657, 646, 642, 659,
639, 644, 650, 718, 658, 722, 637, 635, 718, 704,
622, 661
```

## 6. 两类订单对比

| 项目 | 较快订单 | 较慢订单 | 判断 |
|---|---:|---:|---|
| 检测耗时范围 | 169～290 ms | 619～886 ms | 差异明显 |
| DI1停车等待 | 无 | 46～157 ms | 慢订单会在DI1短暂停车 |
| DI1→DI7范围 | 953～1094 ms | 860～1016 ms | 都能落入同一公共窗口 |

慢订单在DI1等待时，皮带关闭，这段等待不会累计到 `travel_ms`。恢复运行后才开始累计出口运行时间。因此不同订单的视觉耗时主要影响生产节拍和 `inspection_result_wait_timeout_ms`，不会直接要求每个订单使用不同的DI1→DI7窗口。

较慢订单的平均 `travel_ms` 比较快订单短约 `133 ms`，说明两次测试的机械运行状态可能不完全相同，例如皮带速度设定、启停过程或传感器触发位置存在变化。即使如此，两批数据仍能被 `500～1250 ms`公共窗口覆盖。

## 7. `item24 NG → item25 GOOD` 误报警复盘

### 7.1 原始关键日志

```text
2026-09-02 11:31:38 [conveyor] NG item=24 scheduled for blow-off and DI7 reject guard; di1_motion_ms=95077.0; window_ms=500.0..2000.0
2026-09-02 11:31:39 [conveyor] GOOD item=25 passed DI1 and awaits DI7 confirmation; di1_motion_ms=96015.0; window_ms=500.0..3000.0
2026-09-02 11:31:40 [conveyor] DI7 edge: motion_ms=97015.0; active_candidates=[item=24/result=NG/expected=DI7_REJECT_GUARD/elapsed_ms=1938.0, item=25/result=GOOD/expected=DI7/elapsed_ms=1000.0]
2026-09-02 11:31:40 [conveyor] fault REJECT_FAILED_WRONG_OUTLET: NG item 24 candidate reached DI7 after blow-off; DI1_to_DI7_motion_ms=1938.0
```

### 7.2 数值还原

```text
item24 DI1时刻 = 95077 ms
item25 DI1时刻 = 96015 ms
两件产品DI1间隔 = 96015 - 95077 = 938 ms

DI7触发时刻 = 97015 ms
DI7相对item24 = 97015 - 95077 = 1938 ms
DI7相对item25 = 97015 - 96015 = 1000 ms
```

正常产品的实测DI1→DI7范围为 `860～1094 ms`。因此 `1000 ms`与正常运输时间一致，而 `1938 ms`明显偏离正常运输时间。现场又确认item24实际已被吹走，所以该DI7信号应属于item25。

程序之所以报警，是因为item24的NG防错上限仍为 `2000 ms`。DI7到来时距离其上限仅差 `62 ms`，item24和item25同时出现在候选列表中；现有安全逻辑不允许跳过前面的NG，因此按item24报出剔除失败。

### 7.3 对照成功案例：`item45 NG → item46 GOOD`

```text
2026-09-02 11:32:45 [conveyor] NG item=45 scheduled for blow-off and DI7 reject guard; di1_motion_ms=144983.0; window_ms=500.0..2000.0
2026-09-02 11:32:47 [conveyor] NG item=45 reject guard passed without DI7 signal; elapsed_ms=2016.0
2026-09-02 11:32:47 [conveyor] outlet confirmed: item=46, outlet=DI7, travel_ms=1063.0
```

这一组没有误报警，是因为item46到达DI7之前，item45的 `2000 ms`窗口已经结束。它说明当前结果取决于产品节拍是否恰好超过过宽的NG窗口，而不是一个稳定可靠的判断条件。

## 8. 疑似真实剔除失败或额外DI7触发

较慢订单日志中有三次DI7触发时只有NG候选，没有后续GOOD同时处于候选窗口：

| 时间 | 产品 | DI1→DI7相对时间 | 判断 |
|---|---:|---:|---|
| 13:00:58 | item5 NG | 984 ms | 接近正常运输时间，疑似真实剔除失败 |
| 13:09:36 | item24 NG | 828 ms | 只有NG候选，可能是真实剔除失败或额外DI7信号 |
| 13:10:48 | item37 NG | 750 ms | 只有NG候选，可能是真实剔除失败或额外DI7信号 |

这三次与11:31:40的误归属不同，不能简单视为后续OK造成。需要结合现场视频、气阀动作、气压和产品实际去向确认。

## 9. 其他独立异常

### 9.1 DI1触发但FIFO为空

```text
2026-09-02 13:01:51 [conveyor] fault FIFO_UNDERFLOW: DI1 triggered while FIFO is empty
2026-09-02 13:08:41 [conveyor] fault FIFO_UNDERFLOW: DI1 triggered while FIFO is empty
```

可能原因：

1. 两个产品经过DI0时过近，DI0没有恢复无效，两个产品只生成一条记录。
2. DI0漏触发或信号电平/去抖配置不正确。
3. DI1多触发、抖动或传感器被产品重复遮挡。
4. 产品从DI0之后人工放入，或者中途人工移动产品。

这类异常会直接破坏产品与FIFO编号的对应关系，必须独立排查，不能通过调整DI7窗口解决。

### 9.2 DI7触发但没有出口候选

```text
2026-09-02 11:30:58 [conveyor] fault UNEXPECTED_GOOD_OUTLET: DI7 triggered without an eligible outlet expectation
2026-09-02 13:03:19 [conveyor] fault UNEXPECTED_GOOD_OUTLET: DI7 triggered without an eligible outlet expectation
2026-09-02 13:06:30 [conveyor] fault UNEXPECTED_GOOD_OUTLET: DI7 triggered without an eligible outlet expectation
```

可能原因包括残留产品、清线未覆盖的产品、人工中途放料、DI7误触发，或者前序FIFO已经错位。

## 10. 260mm距离与“下一次DI1结束前一NG”的关系

现场确认DI1到DI7约为 `260 mm`，且该距离大于产品最小节距。设：

```text
L = DI1到DI7距离
P = 两件产品相同边沿之间的节距
v = 皮带速度
```

则：

```text
前一件漏吹NG到达DI7所需时间 = L / v
后一件产品到达DI1所需时间 = P / v
```

因为现场 `L > P`，后一件产品可能先到DI1，前一件漏吹NG随后才到DI7。因此不能无条件使用“下一次DI1触发就结束前一个NG防错”的逻辑，否则可能把真正漏吹的NG当成后续GOOD。

正确方法是在每件产品的DI1时刻基础上，用经过现场标定的窄到达窗口判断DI7；若不同产品窗口无法分开，则必须使用DI8逐件确认、编码器位置跟踪或增加产品节距。

## 11. 建议参数

根据当前两类订单、当前测试速度下的59个正常DI7样本，第一轮建议值为：

```json
{
  "good_outlet_arrival_min_run_ms": 500,
  "good_outlet_arrival_max_run_ms": 1250,
  "waste_outlet_arrival_min_run_ms": 500,
  "waste_outlet_arrival_max_run_ms": 1250
}
```

参数作用：

- `good_outlet_arrival_max_run_ms=1250`：GOOD超过1250ms仍未到DI7时尽快报警。
- `waste_outlet_arrival_max_run_ms=1250`：NG在约860～1094ms可能到达DI7，因此真实漏吹仍能被发现；后续GOOD在相对前一NG约1938ms到达时，前一NG窗口已经结束，不再发生本次误归属。
- 保留下限 `500 ms`：覆盖日志中750ms、828ms等较早DI7异常，避免缩窄过度。

这是一组现场试验建议值，不应仅凭本文直接定版。若皮带速度改变，需要重新验证；若所有速度和订单无法被同一窗口覆盖，应按速度选择参数或改成编码器距离跟踪。

## 12. 吹气间距保护参数

当前默认吹气相关参数为：

```json
{
  "reject_blow_delay_ms": 0,
  "reject_blow_duration_ms": 300,
  "reject_following_item_guard_ms": 100
}
```

当前吹气前要求的后续产品最小DI0运动时间间隔为：

```text
0 + 300 + 100 = 400 ms
```

如果NG到达DI1时，后一个已登记产品的DI0间隔小于400ms，程序不吹气、立即停线，并报 `PRODUCT_SPACING_TOO_SMALL`，避免一次吹走两件产品导致FIFO错位。

该保护与DI1→DI7出口窗口是两个不同问题：

- `reject_*` 参数负责防止吹气影响后一个产品。
- `*_outlet_arrival_*` 参数负责判断DI7/DI8信号属于哪件产品。

## 13. 后续复测方案

### 13.1 日志准备

1. 在软件中选择“帮助 → 详细相机诊断（30分钟）”。
2. 确认日志出现：

```text
[camera-diagnostic] detailed=enabled duration_seconds=1800
```

3. 正式版日志文件位于EXE同级的：

```text
logs/runtime_camera.log
```

### 13.2 测试工况

至少覆盖：

1. 检测最快订单。
2. 检测最慢订单。
3. 最低皮带速度。
4. 最高皮带速度。
5. DI1连续运行通过。
6. 产品在DI1停车等待检测结果后重新启动。
7. 最小允许产品节距下的 `NG → GOOD`。
8. 连续 `NG → NG → GOOD`。
9. 可控条件下验证一次真实剔除失败，确认DI7能在1250ms内捕获。

### 13.3 通过标准

1. 所有正常GOOD的 `travel_ms` 均在公共窗口内。
2. 成功吹走的NG不会占用窗口直到后续GOOD到达。
3. 真实漏吹NG到达DI7时必须报警。
4. DI7事件不得同时出现两个已经达到下限且仍未超过上限的候选产品。
5. 不得出现 `FIFO_UNDERFLOW`；若出现，应先解决DI0/DI1逐件触发问题。

## 14. 后续查看日志时的搜索词

```text
travel_ms=
inspection_wait_ms=
DI7 edge:
active_candidates=
reject guard passed
REJECT_FAILED_WRONG_OUTLET
FIFO_UNDERFLOW
UNEXPECTED_GOOD_OUTLET
PRODUCT_SPACING_TOO_SMALL
```

## 15. 当前判断边界

1. 本文没有现场视频，三次只有NG候选的DI7报警不能仅凭日志断定是气阀失败还是DI7额外触发。
2. 运动计时基于皮带运行输出状态，不是编码器实际位移；如果电机加减速明显，命令运行时间与真实移动距离会存在误差。
3. 第二份日志包含多次清线和重新开始，产品编号重复属于不同epoch/测试批次，本文统计按日志时段区分。
4. 若DI8能够可靠逐件检测废料，启用 `waste_outlet_confirmation_enabled=true` 比仅依赖DI7无信号防错更可靠。

